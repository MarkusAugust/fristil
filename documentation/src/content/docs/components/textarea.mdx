---
title: Textarea
description: Flerlinje tekstfelt. pure-css med valid/invalid state.
---

import { TabItem, Tabs } from "@astrojs/starlight/components"

`fs-textarea` er en pure-css stil for `<textarea>` med samme state-mønster som input.

## Import

```js
import "@fristil/designsystem/tokens.css"
import "@fristil/designsystem/textarea.css"
```

## Varianter

<Tabs>
  <TabItem label="Eksempel">
    <div class="not-content" style="display: grid; gap: 0.75rem; padding: 1.5rem; background: #ffffff; border-radius: 0.25rem; border: 1px solid #b2b2b2;">
      <textarea class="fs-textarea" placeholder="Standard"></textarea>
      <textarea class="fs-textarea" data-state="invalid">Ugyldig</textarea>
      <textarea class="fs-textarea" data-state="success">Gyldig</textarea>
      <textarea class="fs-textarea" disabled>Disabled</textarea>
    </div>
  </TabItem>
  <TabItem label="Kode">
    ```html
    <textarea class="fs-textarea"></textarea>
    <textarea class="fs-textarea" data-state="invalid"></textarea>
    <textarea class="fs-textarea" data-state="success"></textarea>
    <textarea class="fs-textarea" disabled></textarea>
    ```
  </TabItem>
</Tabs>

## TypeScript-API

```ts
import {
  getTextareaStyleAttributes,
  type TextareaState,
} from "@fristil/designsystem/textarea"

const state: TextareaState = "success"
const fristilAttributes = getTextareaStyleAttributes(state)

// fristilAttributes blir:
// { class: "fs-textarea", "data-state": "success" }
```

## Runtime input (for eksempel fra API)

```ts
import { isTextareaState, getTextareaStyleAttributes } from "@fristil/designsystem/textarea"

const incoming = String(payload.state)
const state = isTextareaState(incoming) ? incoming : "default"
const fristilAttributes = getTextareaStyleAttributes(state)
```
