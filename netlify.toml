[build]
  base = "."
  command = "npm install && npm run -w @fristil/designsystem build && npm run -w @fristil/documentation build"
  publish = "documentation/dist"

[build.environment]
  NODE_VERSION = "22.23.0"
