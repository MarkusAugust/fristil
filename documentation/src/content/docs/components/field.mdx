---
title: Field (Light DOM)
description: Light DOM wrapper som kobler label, control, help og error automatisk for bedre tilgjengelighet.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

`<ds-field>` er en light-dom komponent bygget med Lit. Den rendrer i vanlig DOM og bevarer native input/select/textarea, men håndterer a11y-wiring.

## Hvorfor `defineDsField()`?

`defineDsField()` registrerer custom elementet i nettleseren:

- Før registrering: `<ds-field>` er bare en ukjent HTML-tag.
- Etter registrering: `<ds-field>` blir en ekte web component med logikken sin.

Du trenger å kalle `defineDsField()` **én gang** når appen starter.
Hvis du kaller den flere ganger, skjer ingenting (den er beskyttet mot dobbel registrering).

## Import

```ts
import { defineDsField } from "@fristil/designsystem/field"
import "@fristil/designsystem/label.css"
import "@fristil/designsystem/input.css"
import "@fristil/designsystem/help-text.css"
import "@fristil/designsystem/error-text.css"

// Kall én gang i app-entry (main.ts, app.ts, e.l.)
defineDsField()
```

## Kom i gang (3 steg)

1. Importer CSS-filene du trenger (`label`, `input`, `help-text`, `error-text`).
2. Kjør `defineDsField()` én gang i app-start.
3. Bruk `<ds-field>` rundt `label` + ett felt (`input`, `select` eller `textarea`).

## Slik ser det ut

<Tabs>
  <TabItem label="Forhåndsvisning">
    <div class="not-content" style="display: grid; gap: 1rem; max-width: 34rem; padding: 1.5rem; background: #ffffff; border: 1px solid #b2b2b2; border-radius: 0.25rem;">
      <div style="display: grid; gap: 0.25rem;">
        <label class="ds-label" data-required="text" for="preview-email">E-post</label>
        <input id="preview-email" class="ds-input" type="email" data-state="invalid" aria-invalid="true" aria-describedby="preview-email-help preview-email-error" value="ola@" />
        <p class="ds-help-text" id="preview-email-help">Vi sender aldri spam.</p>
        <p class="ds-error-text" id="preview-email-error">Skriv en gyldig e-post.</p>
      </div>

      <div style="display: grid; gap: 0.25rem;">
        <label class="ds-label" data-optional="" for="preview-message">Melding</label>
        <textarea id="preview-message" class="ds-textarea" rows="3" placeholder="Skriv en kort melding"></textarea>
        <p class="ds-help-text">Du kan skrive opptil 500 tegn.</p>
      </div>
    </div>
  </TabItem>
  <TabItem label="Med ds-field">
    ```html
    <ds-field required-marker="text" invalid>
      <label>E-post</label>
      <input class="ds-input" type="email" value="ola@" />
      <p class="ds-help-text">Vi sender aldri spam.</p>
      <p class="ds-error-text">Skriv en gyldig e-post.</p>
    </ds-field>

    <ds-field optional>
      <label>Melding</label>
      <textarea class="ds-textarea" rows="3" placeholder="Skriv en kort melding"></textarea>
      <p class="ds-help-text">Du kan skrive opptil 500 tegn.</p>
    </ds-field>
    ```
  </TabItem>
</Tabs>

Komponenten gjør dette automatisk:

1. Setter `label[for]` til control-id.
2. Genererer id på help/error ved behov.
3. Setter `aria-describedby` på kontrollen.
4. Setter `aria-invalid="true"` når `invalid` er satt.
5. Setter `data-required`/`data-optional` på label via wrapper-attributter.
6. Viser eller skjuler `ds-error-text` automatisk basert på `invalid`.

## Minste eksempel

```html
<ds-field>
  <label>Navn</label>
  <input class="ds-input" type="text" />
</ds-field>
```

Dette gir deg automatisk kobling mellom label og input uten at du manuelt må holde styr på `for`/`id`.

## Bruk i en app

<Tabs>
  <TabItem label="Vanilla JS">
    ```js
    // main.js – kjør én gang ved oppstart
    import { defineDsField } from '@fristil/designsystem/field'
    import '@fristil/designsystem/label.css'
    import '@fristil/designsystem/input.css'
    import '@fristil/designsystem/help-text.css'
    import '@fristil/designsystem/error-text.css'

    defineDsField()
    ```

    ```html
    <form id="contact-form">
      <ds-field id="name-field" required-marker="symbol">
        <label>Navn</label>
        <input class="ds-input" type="text" name="name" />
        <p class="ds-error-text">Navn er påkrevd.</p>
      </ds-field>
      <button class="ds-button" type="submit">Send</button>
    </form>

    <script type="module">
      const form = document.getElementById('contact-form')
      const nameField = document.getElementById('name-field')

      form.addEventListener('submit', (e) => {
        e.preventDefault()
        const input = nameField.querySelector('input')
        if (!input.value.trim()) {
          nameField.setAttribute('invalid', '')
        } else {
          nameField.removeAttribute('invalid')
          // prosesser skjemaet...
        }
      })
    </script>
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { useState } from 'react'

    export function ContactForm() {
      const [name, setName] = useState('')
      const [touched, setTouched] = useState(false)

      const invalid = touched && !name.trim()

      return (
        <form onSubmit={(e) => e.preventDefault()}>
          <ds-field required-marker="symbol" invalid={invalid}>
            <label>Navn</label>
            <input
              className="ds-input"
              type="text"
              name="name"
              value={name}
              onChange={(e) => setName(e.target.value)}
              onBlur={() => setTouched(true)}
            />
            <p className="ds-error-text">Navn er påkrevd.</p>
          </ds-field>
          <button className="ds-button" type="submit">Send</button>
        </form>
      )
    }
    ```
  </TabItem>
</Tabs>

## Attributter

| Attributt | Type | Effekt |
|---|---|---|
| `invalid` | boolean | Setter `aria-invalid` og invalid state på kontroll |
| `disabled` | boolean | Setter disabled/aria-disabled på kontroll og label |
| `optional` | boolean | Setter `data-optional` på label |
| `required-marker` | `none \| symbol \| text` | Setter `data-required` på label |
| `control-id` | string | Tvinger id på kontroll |
| `described-by` | string | Legger til ekstra `aria-describedby`-id(er) |

## Når vises feilmeldingen?

Feilmeldingen vises bare når `invalid` er satt på `<ds-field>`.

Det betyr:

- `invalid` = `false` eller ikke satt: `ds-error-text` skjules.
- `invalid` = `true`: `ds-error-text` vises og kobles til feltet via `aria-describedby`.

Eksempel:

```html
<ds-field invalid>
  <label>E-post</label>
  <input class="ds-input" type="email" value="ola@" />
  <p class="ds-error-text">Skriv en gyldig e-post.</p>
</ds-field>
```

Når feltet ikke er invalid, trenger du fortsatt å ha feilmeldingselementet i markup hvis du vil bruke samme struktur senere. Komponenten skjuler det for deg til det faktisk trengs.
